#include <iostream>
using namespace std;

int main(){

    //Arthimetic Operator ( + , - , * , / ,  % )
    // int a = 10, b = 5;
    // cout << "Sum = " << (a+b) << endl;
    // cout << "difference = " << (a-b) << endl;
    // cout << "product = " << (a*b) << endl;
    // cout << "division = " << (a/b) << endl;
    // cout << "modulo = " << (a%b) << endl;

    // cout << (5 /(float) 2) << endl;

    // Relation Operator ( (<) lese then , (>) grater then , (=) equal to, (<=) , (>=) , (==) )
    // cout << (3 > 5) << endl; // false -> 0 
    // cout << (3 < 5) << endl; // true -> 1 
    // cout << (3 <= 5) << endl; // true -> 1
    // cout << (3 >= 5) << endl; // false -> 0
    // cout << (5 == 5) << endl; // false -> 0
    // cout << (3 != 5) << endl; //true -> 1
    // cout << (3 != 3) << endl; // false -> 0

    //Logical Operator ( OR (||) , AND (&&) , NOT (!))

    cout << (3 > 1) << endl;
    cout << !(3 > 1) << endl;
 
    return 0;
}